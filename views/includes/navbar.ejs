<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top px-3">
  <div class="container-fluid">
    <!-- Brand -->
    <a class="navbar-brand" href="/listings">
      <i class="fa-solid fa-compass fs-4"></i>
    </a>

    <!-- Toggler (Offcanvas) -->
    <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas" data-bs-target="#mobileMenu"
      aria-controls="mobileMenu">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Desktop Navbar -->
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav me-3">
        <a class="nav-link" href="/listings">Explore</a>
        <a class="nav-link" href="/listings/new">Add Listings</a>
      </div>

      <form class="d-flex mx-auto w-50" role="search" action="/listings/search" method="GET">
        <input class="form-control me-2 rounded-pill" type="search" name="q" placeholder="Search listings..." aria-label="Search">
        <button class="nav-btn rounded-pill" type="submit"><i class="fas fa-search"></i></button>
      </form>

      <div class="ms-auto d-flex gap-2">
        <% if (!currUser) { %>
          <form action="/signup" method="GET">
            <button type="submit" class="nav-btn rounded-pill px-3 py-1">Sign Up</button>
          </form>
          <form action="/login" method="GET">
            <button type="submit" class="nav-btn rounded-pill px-3 py-1">Log In</button>
          </form>
        <% } else { %>
          <form action="/logout" method="GET">
            <button type="submit" class="nav-btn rounded-pill px-3 py-1">Log Out</button>
          </form>
        <% } %>
      </div>
    </div>
  </div>
</nav>

<!-- Mobile Offcanvas Menu -->
<div class="offcanvas offcanvas-end" tabindex="-1" id="mobileMenu" aria-labelledby="mobileMenuLabel"
     style="width: 260px; background-color: white;">
  <div class="offcanvas-header border-bottom">
    <h6 class="offcanvas-title" id="mobileMenuLabel">Menu</h6>
    <button type="button" class="btn-close text-reset" data-bs-dismiss="offcanvas" aria-label="Close"></button>
  </div>
  <div class="offcanvas-body d-flex flex-column gap-3">
    <a class="nav-link" href="/listings">Explore</a>
    <a class="nav-link" href="/listings/new">Add Listings</a>

    <form class="d-flex" role="search" action="/listings/search" method="GET">
      <input class="form-control rounded-pill me-2" type="search" name="q" placeholder="Search listings..." aria-label="Search">
      <button class="btn btn-outline-secondary rounded-pill" type="submit"><i class="fas fa-search"></i></button>
    </form>

    <% if (!currUser) { %>
      <form action="/signup" method="GET">
        <button type="submit" class="nav-btn w-100 rounded-pill">Sign Up</button>
      </form>
      <form action="/login" method="GET">
        <button type="submit" class="nav-btn w-100 rounded-pill">Log In</button>
      </form>
    <% } else { %>
      <form action="/logout" method="GET">
        <button type="submit" class="btn btn-outline-danger w-100 rounded-pill">Log Out</button>
      </form>
    <% } %>
  </div>
</div>
